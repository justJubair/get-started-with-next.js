const TestimonialPage = () => {
    return(
        <div>
             <p>Contact page with header and footer, About page with header and footer </p>
        </div>
    )}
export default TestimonialPage;